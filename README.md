# Git Commands
## Add a file: 
`git add <filename>`
## Check Status:
`git status`
## Commit Changes:
`git commit -m 'my commit message here'`
## git push (submits code to github):
`git push`
## git pull (grabs code from github):
`git pull`

